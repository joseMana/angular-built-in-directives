import { PrimaryHighlightDirective } from './primary-highlight.directive';

describe('PrimaryHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new PrimaryHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
